{{#each users as |user|}}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{user.name}} test</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:test@gmail.com">{{user.email}} em</a></h3>
  </div>
</div>
{{/each}}